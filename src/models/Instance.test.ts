import * as Spawnkit from "@/.";
import wait from "wait";

describe("Instance", () => {
  it.todo(
    "should wait for the snapshot to be loaded before starting processing events",
  );

  it.todo("should stay alive while a streaming is happening");
});
