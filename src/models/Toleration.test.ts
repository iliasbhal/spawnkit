import { Toleration } from "@/models/Toleration";

describe("Toleration", () => {
  it.todo("should match empty traits");
  it.todo("should not match if required trait is missing");
  it.todo("should favor when a prefer affinity is matched");
});