import { Data } from "./Data";

describe("Data", () => {
  it.todo("should be able to set and get data");
  it.todo("should return null if data is not set yet");
  it.todo("should cache data per key");
  it.todo("should only call set once per tick max");
});
